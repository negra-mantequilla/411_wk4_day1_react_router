import React from "react";
import cars from "../cars.json";
// read the props.match.params.id (how to filter an array)
// need to filter the array based on the id you are getting 
// id found in the object
// display the car info usin Material chips 


// import material ui components here //
// Container, Paper, Chip //
// import {useParams} from "react-router-dom"

const Car = (props) => {
  return <h1>A specific car</h1>;
};

export default Car;
